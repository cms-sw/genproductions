

Set the correct compiler, paths and flags in the makefile here.
QDHOME should be the the qd-directory.
FC, CXX, FCFLAGS, FCLIBS should correspond to the values presented
to you when you built the arprec package.

Then execute "make" followed by "./f_test.exe < input"


