--- a/madgraph/various/cluster.py	2018-03-16 05:03:58.149058939 -0500
+++ b/madgraph/various/cluster.py	2018-03-16 05:03:30.174215070 -0500
@@ -1006,7 +1006,9 @@
         
         if self.cluster_queue not in ['None', None]:
             requirement = 'Requirements = %s=?=True' % self.cluster_queue
-        else:
+        elif "uwlogin" in os.environ["HOSTNAME"] or "hep.wisc.edu" in os.environ["HOSTNAME"]:
+            requirement = 'Requirements = Site == "HEP"'
+        else
             requirement = ''
 
         if cwd is None:
@@ -1082,7 +1084,9 @@
         
         if self.cluster_queue not in ['None', None]:
             requirement = 'Requirements = %s=?=True' % self.cluster_queue
-        else:
+        elif "uwlogin" in os.environ["HOSTNAME"] or "hep.wisc.edu" in os.environ["HOSTNAME"]:
+            requirement = 'Requirements = Site == "HEP"'
+        else
             requirement = ''
 
         if cwd is None:
